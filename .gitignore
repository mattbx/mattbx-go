# Secrets
.env

# SQLite data (lives on a Disco volume in production)
/data/
*.db
*.db-shm
*.db-wal

# Build artifacts
/tmp/
/build/

# templ codegen — regenerated by scripts/local.sh and the Dockerfile
*_templ.go

# Tailwind output — compiled by scripts/local.sh and the Dockerfile
/internal/ui/static/tailwind.css

# Working notes — local only, not part of the product
LEARNINGS.md
CLAUDE.local.md
DEPLOY.local.md
disco-llms-reference.txt

# macOS
.DS_Store
